services:
  jupyter_bci:
    build: .
    container_name: EEGNet-Motor-Imagery-BCI
    ports:
      - "8888:8888"
      - "5001:5000"
    volumes:
      - .:/app
    environment:
      - JUPYTER_ENABLE_LAB=yes
      - MLFLOW_TRACKING_URI=sqlite:////app/mlflow.db